---

django_directories: []

django_settings_templates: []

django_settings: ""

django_virtualenv: ""

django_pre_commands: []

django_main_commands:
  - command: migrate
    run_once: true
  - collectstatic

django_post_commands: []

django_run_once_host: "{{ ansible_play_hosts_all[0] }}"
